# Drainpad — the X profile

English throughout, like everything this account puts out.

## Ticker

`$SUMP`.

## Handle

`@drainpadfun` if it is free. `@drainpad_fun` and `@drainpaddotfun` are the
fallbacks, in that order. Avoid anything carrying a number — a handle with a
digit in it reads as the impersonation account rather than the real one.

## Display name

```
Drainpad
```

Just the word. No ticker, no emoji, no "| Robinhood Chain". The name is the one
place where looking like everyone else costs the most.

## Bio

A bio is a different limit from a post: **160 characters**, and X does not
shorten a link inside one, so the domain is counted in full. All three below are
measured.

The one that ships, 149:

```
A launchpad on Robinhood Chain. One transaction: the token is minted, the pool opens, and the pool's share drops where there is no pump.
drainpad.fun
```

Two alternates, 90 and 112, if that reads long on a phone:

```
Water finds the lowest point and stays there. A launchpad on Robinhood Chain.
drainpad.fun
```

```
What goes down the drain does not come back up. Launch a token in one transaction. Robinhood Chain.
drainpad.fun
```

## Images

| Surface | File | Made by |
| --- | --- | --- |
| Avatar | `out/x-avatar-1000x1000.jpg` | Flow art, cropped square by `compose.sh` |
| Header | `out/x-header-1500x500.jpg` | Flow art, captioned by `compose.sh` |
| Link preview | `out/og-1200x628.jpg` | Drawn by `compose.sh`, served by the site |

## Pinned post

The launch announcement, once it is up. Nothing else is worth the slot until
there is a token to point at.

## What never goes in the bio

The supply, the split, what the grate keeps, and the range a pool opens across.
They are constants in verified contracts, and a bio is the worst place to keep a
second copy of a number — it cannot be checked against the chain by reading it,
and it is the first thing to go stale.
